import {Styles} from '@ijstech/components';
import LightTheme from './light.theme';
import DarkTheme from './dark.theme';

const currentTheme: Styles.Theme.ITheme = LightTheme;

export { currentTheme, LightTheme, DarkTheme };
